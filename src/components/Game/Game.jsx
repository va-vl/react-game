import React from 'react';
import { Typography } from '@material-ui/core';

const Game = () => {
  const a = 'This is a game';

  return (
    <Typography>{a}</Typography>
  );
};

export default Game;
